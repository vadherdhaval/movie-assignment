import styled from 'styled-components';

export const Container = styled.div`
   display: flex;
   align-items: center;
   min-height: 20px;
   font-size: 20px;
`
export const Rating = styled.div`
    cursor: pointer;
`